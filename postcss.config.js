// postcss.config.js
module.exports = {
  plugins: [
    require("tailwindcss"), // Aggiungi il plugin di Tailwind
    require("autoprefixer"), // Autoprefixer è usato per aggiungere i prefissi CSS automaticamente
  ],
};
